# Correction rules

Accumulated corrections from the user. One rule per line. Applied on every draft
(plan §8). New sessions and later stages inherit these automatically.

- Do not treat a registration/submission deadline (报名截止) as the event start date.
- Always reply in English and open with a greeting line (e.g. "Hi <name>,").
